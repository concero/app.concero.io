.avatar {
	width: 100%;
	height: 100%;
	border-radius: var(--space-xxxxxxl);
}
.default_avatar {
	width: 100%;
	height: 100%;
	border-radius: var(--space-xxxxxxl);
	background-color: #d9d9d9;
}

.hovered {
	outline: 2px solid var(--color-accent-500);
	outline-offset: 2px;
}
