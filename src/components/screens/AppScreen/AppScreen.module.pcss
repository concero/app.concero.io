.container {
	background-color: var(--color-base-background);
	flex: 1;
	padding: var(--sp-screen-v) var(--sp-screen-h);
}

.abTestNewSwapCardLayoutContainer {
	background-color: var(--color-grey-650);
}

@media (max-width: 768px) {
	.container {
		padding: var(--sp-sm);
	}
}
