.container {
	flex: 1;
	gap: var(--sp-md);
}

.stepBlock {
	gap: var(--sp-md);
}

.buttonContainer {
	width: 100%;
	flex-direction: row;
	gap: var(--sp-md);
}

.f1 {
	flex: 1;
}

.title {
	color: var(--color-text-secondary);
}

.alightStart {
	align-self: flex-start;
}
