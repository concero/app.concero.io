.container {
	width: 35%;
	gap: var(--sp-sm);
}

.amount {
	height: 24px;
	width: 100%;
	border-radius: var(--st-br-sm);
}

.amountUsd {
	height: 20px;
	width: 40%;
	border-radius: var(--st-br-sm);
}
