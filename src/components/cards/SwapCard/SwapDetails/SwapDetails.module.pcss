.swapDetailsContainer {
	height: 100%;
	padding: var(--sp-md) 0;
	align-items: center;
	flex-direction: row;
	justify-content: space-between;
	overflow: hidden;

	.swapTagContainer {
		flex-direction: row;
		gap: var(--sp-xs);

		.swapPriceTag {
			align-items: center;
			flex-direction: row;
			gap: var(--sp-xs);
		}
	}
}

.routeCardsContainer {
	overflow-y: scroll;
	gap: var(--sp-sm);
}
