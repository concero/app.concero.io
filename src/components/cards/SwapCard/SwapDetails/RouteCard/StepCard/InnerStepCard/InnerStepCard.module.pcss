.container {
}

.rowContainer {
	flex-direction: row;
	align-items: center;
	gap: var(--sp-xs);
}

.tagContainer {
	flex-direction: row;
	align-items: center;
	gap: 2px;
}

.title {
	font-size: 14px;
	font-weight: 400;
}
