.cardContainer {
  width: 100%;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
}

.avatarContainer {
  flex-direction: row;
  align-items: center;
  gap: var(--sp-sm);
}
