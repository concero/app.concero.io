.modal {
	padding: 0;
}

.container {
	width: 450px;
	flex-direction: row;
}

.tokenContainer {
	margin-top: var(--sp-md);
	gap: var(--sp-md);
	overflow-y: scroll;
	height: 434px;
}

.tokensContainer {
	width: 100%;
	padding: 0 var(--sp-md);
}

.header {
	padding: var(--sp-md) 0;
	flex-direction: row;
	justify-content: space-between;
}

@media (max-width: 768px) {
	.container {
		width: 100%;
	}
}
