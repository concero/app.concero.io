.container {
	width: 100%;
	padding-top: var(--sp-md);
	padding-bottom: var(--sp-md);
	gap: var(--sp-sm);
}

.chainsContainer {
	flex-direction: row;
	flex-wrap: wrap;
	overflow: hidden;
	gap: var(--sp-sm);
}

.firsChainsLineContainer {
	flex-direction: row;
	flex-wrap: nowrap;
	gap: var(--sp-sm);
}
