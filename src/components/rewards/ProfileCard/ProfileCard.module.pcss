.container {
	gap: var(--st-br-md);
	overflow: hidden;
	background: var(--color-grey-25);
}

.header {
	display: flex;
	flex-wrap: wrap;
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
	gap: var(--sp-lg);
}

.avatar {
	border-radius: 50%;
	width: 32px;
	height: 32px;
}

@media (max-width: 768px) {
	.container {
		width: 100%;
	}
}
