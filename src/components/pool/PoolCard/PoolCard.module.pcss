.container {
	width: 100%;
	max-width: 390px;
	margin-top: 48px;
}

.buttons {
	width: 100%;
	flex-direction: row;
	gap: var(--sp-sm);
}

.tooltip {
	width: 196px;
}

@media (max-width: 744px) {
	.buttons {
		flex-direction: column;
		width: 100%;
	}
}
