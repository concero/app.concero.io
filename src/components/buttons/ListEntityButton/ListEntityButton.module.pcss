.selectItemContainer {
	width: 100%;
	justify-content: space-between;
	align-items: center;
	flex-direction: row;

	.infoContainer {
		gap: var(--sp-sm);
		flex-direction: row;
		align-items: center;
	}
}
