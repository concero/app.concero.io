.controls {
	display: flex;
	flex-direction: row;
	gap: var(--space-xs);
}

.full_width {
	width: 100%;
}
.alert {
	width: 100% !important;
	flex-direction: row;

	& > div:last-child {
		width: 100%;
	}
}
