.container {
	flex-direction: row;
	align-items: center;
	height: 100%;
	gap: var(--sp-md);
}

.button_wallet {
	min-width: 154px;
	justify-content: space-between !important;
	&.is_connected {
		& > span:last-child {
			margin-left: auto;
		}
	}
}
